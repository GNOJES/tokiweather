# AGENTS.md

> **Notice for AI Agents (Antigravity, Codex, Claude, etc.)**
> 이 문서는 세션이 종료되거나 다른 AI 에이전트로 작업 주체가 변경되어도 프로젝트의 맥락, 사용자 의도, 제약사항, 과거 시행착오를 놓치지 않도록 작성된 **인수인계 문서**입니다.
> **향후 구조, 규칙, 주요 의사결정이 변경되면 본 문서(`AGENTS.md`)도 반드시 함께 갱신해야 합니다.**

---

## Project context

- **앱 이름**: 토끼날씨 (TokiWeather)
- **플랫폼 & 기술 스택**: Android (Kotlin), Jetpack Compose, Jetpack Glance (AppWidget), DataStore, Retrofit2/OkHttp, Material 3
- **현재 버전**: `0.9.2` (versionCode `10`), `main` 브랜치 운용 중
- **핵심 기능**:
  1. 기상청 실시간 날씨 및 한국환경공단 에어코리아 대기질(PM10·PM2.5) 데이터 연동. 대기질은 현재 위치에서 가까운 측정소의 시간별 실측값 사용.
  2. 홈 화면 위젯 2종 제공: One UI 4×7(Glance 2×1) 및 노바런처 8×8(Glance 3×2)
  3. 앱 메인 3탭 구조: 날씨(종합 예보) / 초단기(네이버 초단기강수예측 웹뷰) / 설정(위젯 및 갱신 주기 설정)

---

## Important decisions

1. **하단 내비게이션 바(3탭) UI/UX 규칙**:
   - **텍스트 라벨 완전 제거**: 탭 하단 글자("날씨", "초단기", "설정")는 제거하고, 아이콘을 48dp로 최대화하여 미니멀하고 시원한 룩 유지.
   - 탭 콘텐츠 높이는 선택 캡슐과 같은 64dp이며 시스템 내비게이션 영역 위에 둔다. 불필요한 상하 여백은 추가하지 않는다.
   - **탭별 선택/미선택 상태 비주얼 룰**:
     - **미선택 상태**: 순수 테마 오브젝트 단독 노출 (100% 선명도/불투명 유지, 흐리게 처리 금지).
     - **선택 상태**: 테마 오브젝트 + 토끼날씨 캐릭터(Toki) 함께 노출 + 넓은 파스텔 라벤더 배경 캡슐(`112×64dp`, `primary.copy(alpha = 0.18f)`). 터치 시 별도의 회색 눌림 표시·리플이 겹쳐 보이지 않도록 탭의 시각적 indication은 제거하고, 선택 상태 캡슐만 표시한다. 탭 전체의 터치 영역과 접근성 선택 상태는 유지한다.
   - **개별 탭 아이콘 세부 형태**: 승인된 3D 시안을 `res/drawable-nodpi/ic_tab_*.png` 6종(각 탭의 선택/미선택)으로 적용. 토끼는 선택 상태마다 얼굴만이 아닌 몸 전체가 보이도록 한다.
     - **날씨 (Tab 0)**:
       - 미선택: 기존 `ic_weather_cloudy.png`의 둥근 3D 태양 + 웃는 구름 디자인을 계승.
       - 선택: 기존 `ic_weather_clear.png`의 3D 맑은 태양 + 오른쪽에 몸 전체가 보이는 3D 토끼. 태양 얼굴을 가리지 않는다.
     - **초단기 (Tab 1)**:
       - 미선택: 얼굴 표정이 있는 파스텔 하늘색 3D 우산 + 하단에 이격된 물방울.
       - 선택: 우산 표정을 유지하고 좌측 아래에 몸 전체가 보이는 3D 토끼를 배치. 물방울은 우산과 자연스럽게 이격.
     - **설정 (Tab 2)**:
       - 미선택: 파스텔 라일락 3D 기계식 톱니바퀴 (6개 명확한 사다리꼴 돌기 + 중앙 홀 + 베벨 링 + 골드 반짝이).
       - 선택: 톱니바퀴 + 좌측에서 인사하는 몸 전체의 3D 토끼 + 골드 반짝이.
   - **날씨 탭 카드 순서**: 큰 현재 날씨·습도 카드 → 기상청 시간별 예보 가로 스크롤(1시간/3시간 선택) → 기상청 내일·모레 오전·오후 일별 예보(두 날짜를 한 화면에 나란히 표시) → 실시간 대기질 → 기상청 링크. Galaxy S23 기본 글자 크기에서는 이 카드들을 세로 스크롤 없이 한 화면에 보이도록 간격을 유지한다.
2. **진입 및 뒤로가기 동작**:
   - 앱 실행 및 위젯 클릭 시 항상 **0번(날씨) 탭**으로 기본 진입.
   - 1번(초단기), 2번(설정) 탭에서 뒤로가기 누르면 **0번(날씨) 탭**으로 이동. 0번 탭에서 뒤로가기 누르면 앱 종료 (`MainActivity.kt`의 `BackHandler`).
3. **위젯 설정 화면 (Tab 2)**:
   - "저장 및 새로고침" 버튼 클릭 시 위젯 전체 갱신(`TokiWeatherWidget.updateAll()`) 후 `finish()`로 앱 종료.
   - 갱신 주기 옵션(15분/30분/1시간)에서 `(추천)` 텍스트 제거 및 선택된 칩의 시인성 높은 테두리/배경 적용.
   - 스크롤 영역의 맨 아래에 `BuildConfig.VERSION_NAME`으로 앱 버전을 표시한다.
4. **파스텔톤 컬러 원칙 (하단 탭 아이콘에만 적용)**:
   - 이 원칙은 위젯 배경색에는 적용하지 않으며, 기존 짙은 보라색 위젯 배경을 유지할 수 있음.
   - 어둡거나 진한 원색(예: 진보라 `#5E35B1`, 진남색 `#1976D2`, 짙은 슬레이트 `#37474F` 등)은 절대 지양.
   - 부드러운 스카이블루, 라일락, 파스텔 옐로우, 소프트 핑크 등 화사하고 귀여운 파스텔톤 일관성 유지.
5. **날씨 갱신과 캐시의 신뢰성**:
   - 두 위젯은 같은 주기 작업을 공유한다. 한 종류의 위젯을 제거해도 다른 종류가 남아 있으면 예약을 취소하지 않는다. 새 위젯 추가 시 저장된 15/30/60분 주기를 적용한다. 기존 위젯이 있는 상태에서 앱 업데이트/실행 후 예약이 누락됐으면 저장된 주기로 복구하고, 이미 살아 있는 예약은 재설정하지 않는다.
   - 수동 갱신은 `fetchAndSave()` 성공을 확인한 뒤에만 성공으로 안내하고 위젯을 갱신한다. 설정 저장 후 날씨 요청만 실패하면 설정 저장 사실과 갱신 실패를 구분해 안내한다.
   - 기상청 응답 코드와 현재 기온(T1H)을 검증한다. 오류 응답/필수값 누락 시 이전 캐시를 보존하며, 내일·모레 최저/최고가 누락되면 0°C 대신 `—`로 표시한다. 실제 0°C는 정상 값이다.
   - 날씨 탭 상단은 현재 관측 날씨·기온과 초단기실황 습도(REH)를 표시한다. 시간별 예보는 단기예보(VilageFcst)의 SKY/PTY/TMP/POP를 기본으로, 가까운 약 6시간은 초단기예보(UltraSrtFcst)의 SKY/PTY/T1H/POP로 덮어쓴다. 초단기예보가 실패하면 단기예보로 표시한다. 오늘 최고 강수확률은 이 병합 결과의 남은 시간대 최댓값이며 위젯도 같은 캐시값을 사용한다. 시간별 1시간/3시간 선택은 DataStore에 보존하고, 3시간 표시는 각 구간의 최고 강수확률과 기온 범위를 묶는다. 내일·모레 일별 예보는 단기예보를 오전(06~12시)·오후(12~18시)로 집계한다. 각 구간의 아이콘은 최고 강수확률 시간대의 SKY/PTY, 기온은 구간 내 TMP 범위, 강수확률은 구간 최댓값을 표시한다. 위젯은 오전/오후로 분리하지 않는다. 위젯의 오늘 아이콘과 기온은 현재 상태, 내일·모레 아이콘은 일별 최고 강수확률 시간대의 예보를 사용한다.
   - 초단기예보는 현재 시각의 정시 자료를 먼저 요청하고 아직 없거나 실패한 경우 직전 시각을 재시도한다. 화면의 발표 시각은 응답의 실제 `baseTime`을 쓰며 시간별 예보 제목 오른쪽에 `19:00 발표`처럼 표시한다. 별도의 설명 줄은 두지 않는다. 3시간 모드는 00·03·06…21시 기준으로 표시하며 날짜가 바뀌는 곳에 구분선을 둔다. 일별 예보의 각 날짜 안에서는 오전을 왼쪽, 오후를 오른쪽에 배치한다. 일별 아이콘은 33dp, 오전·오후와 강수확률은 시간별 예보와 같은 11sp로 표시한다. 현재 날씨의 최고 강수확률은 파란 물방울 아이콘과 수치로 표시한다.
   - 날씨 탭의 현재 날씨와 시간별 예보에서는 위치·날짜별 태양 위치를 계산해 일몰 후/일출 전 맑음과 구름많음에 각각 밝은 청회색 달, 구름+밝은 청회색 달 아이콘을 쓴다. 흐림·강수 아이콘과 위젯의 아이콘은 그대로 둔다.
   - 앱 날씨 탭 우측 상단 새로고침은 캐시 저장 후 두 종류의 위젯도 갱신한다. 최초 앱 실행 시 캐시가 비어 있으면 한 번 조회한다. 에어코리아의 일시적 네트워크 실패는 한 번 재시도하되 결측·점검값을 정상 수치로 대체하지 않는다. 위젯 설정 저장·갱신 중에는 딤드 로딩을 보이고 중복 탭과 뒤로가기를 막는다.
   - 2026-09-25 재발 원인: 에어코리아 측정소 목록 요청이 두 차례 시간 초과된 뒤 `-1/-1`이 기존 정상 대기질 캐시를 덮어썼다. 대기질 조회 자체가 실패하면 이전 관측값을 관측 시각 기준 3시간 이내이고 조회 좌표가 2km 이내일 때만 유지한다. 동 이름은 역지오코딩에 따라 달라질 수 있어 판정에 쓰지 않는다. 관측값이 결측/점검이거나 위치가 멀리 바뀌면 기존 수치를 재사용하지 않는다. 관측 시각이 저장되지 않은 이전 버전 캐시는 보수적으로 재사용하지 않는다.
6. **API 키와 진단 로그**:
   - 기상청과 에어코리아의 인증키가 들어간 요청 URL/본문이나 원본 예외 메시지를 로그 및 사용자 안내에 남기지 않는다.
   - `BuildConfig`에 포함된 클라이언트 키는 APK에서 추출될 수 있다. 공개 배포 전 공공데이터포털 사용량 한도 및 키 교체 절차를 점검한다. 서버 프록시는 실제 남용 문제가 생기면 별도 설계한다.

---

## Constraints and rules

1. **위젯(Glance) 제약**:
   - 위젯은 일반 Jetpack Compose가 아닌 `androidx.glance` 기반 (`TokiWeatherWidget`, `TokiWeatherWidgetLarge`).
   - 일반 Compose 컴포넌트(Canvas, ConstraintLayout, ImageVector 등) 사용 불가. Glance 전용 `Box`, `Row`, `Column`, `Text`, `Image`만 사용 가능.
   - 기상 아이콘은 로컬 정적 리소스(`res/drawable/ic_weather_*.png`)로 번들되어 있어야 함 (Glance 내 비동기 네트워크 이미지 로딩 불가).
2. **Android Vector Drawable 제약**:
   - Android Vector XML은 `<rect>`, `<circle>`, `<polygon>`, `<g>` 태그를 지원하지 않음.
   - 오직 `<vector>`, `<group>`, `<path>`만 유효함. 모든 도형은 `pathData` (SVG path 문법)로 작성해야 함.
3. **하단 내비게이션 바 수정 시 금기사항**:
   - 탭 텍스트 라벨 다시 추가 금지.
   - 미선택 아이콘에 `alpha(0.55f)` 등으로 투명도 감쇄 금지 (배경 대비상 흐려 보임).
   - 선택 상태 캡슐을 너무 연하게(`secondaryContainer alpha 0.75` 등) 설정 금지 (배경과 구분이 안 됨).
4. **Git 및 작업 원칙**:
   - 작업 디렉토리: `/Users/a220330002/Projects/personal/tokiweather` (상위 `Projects/tokiweather` 심링크 또는 직접 접근 확인 필요).
   - 커밋 메시지는 Conventional Commits(`feat:`, `fix:`, `chore:`, `style:`) 형식 준수.
   - 작업 완료 전 반드시 단위 테스트(`testDebugUnitTest`) 및 빌드(`assembleDebug`) 검증 필수.

---

## Known issues / limitations

1. **날씨 탭(Tab 0)의 상세 예보 범위**:
   - 현재 날씨와 습도를 크게 유지하고, 시간별 예보만 가로 스크롤한다. 일별 예보는 오늘을 제외한 내일·모레를 한 카드에 나란히 표시한다. 대기질과 기상청 링크는 그 아래에 둔다.
   - 시간별 꺾은선 차트는 아직 구현하지 않았다. 추후 필요하면 별도로 기획한다.
2. **현재 예보 범위와 확장 계획 (3일 → 7일)**:
   - 현재 앱은 기존 기상청 단기예보(VilageFcst) 연동으로 3일 요약을 제공하며, 추후 7일까지 확장할 계획.
   - 이는 현재 앱의 구현 범위이며, API 자체가 항상 3일치만 제공한다는 의미는 아님. 확장 시 최신 명세를 확인하고 중기예보 API(MidFcst) 등 추가 연동 방식을 결정할 것.
3. **동 이름 역지오코딩 한계 및 개선 (2026-09-24)**:
   - Galaxy S23 실기기에서 GPS 좌표는 약 18~20m 정확도로 안정적이었으나, Android `Geocoder`가 현재 좌표에서 약 350m 떨어진 `영등포동8가` 지번을 첫 후보로 반환함. 결과 1개/5개 조회 모두 같은 첫 후보였음.
   - 앱은 최신 위치(최대 2분) 우선, 가까운 주소 후보(200m 이내)만 동 이름으로 사용하도록 수정. 신뢰할 만한 동 후보가 없으면 구/시 명칭으로 표시해 잘못된 동을 단정하지 않음.
   - 2026-09-24 Galaxy S23 실기기에 수정 APK 설치 후 새로고침: GPS 정확도 약 15m, `WeatherRepository`의 `locationName=영등포동7가` 저장과 화면 상단 `영등포동7가` 표시를 확인. 별도 카카오 API 없이 이번 위치에서 원하는 동 이름이 표시되었음. 다만 Android Geocoder 후보가 모두 먼 주소인 경우에는 구/시 명칭으로 보수적으로 표시하므로, 삼성 날씨와 항상 일치하는 것은 보장하지 않음. 정확한 법정동 경계 판정이 필요해지면 국내 지도 API(예: 카카오 로컬 `coord2regioncode`, REST API 키 필요) 검토.
4. **대기질 일시적 미수신 (2026-09-24 실기기 관찰)**:
   - Galaxy S23 새로고침 1회에서 전체 갱신이 약 32초 걸리고 대기질이 `-1`(자료 없음)으로 저장됨. 당시 구현은 대기질 예외를 로그에 남기지 않아 정확한 실패 단계는 알 수 없지만, 30초 HTTP 읽기 제한에 걸린 일시적 지연 가능성이 큼.
   - 같은 좌표의 가장 가까운 `영등포로` 측정소에서 공식 API는 PM10 32·PM2.5 21의 정상 관측값을 제공했고, 다음 실기기 새로고침에서 약 2초 만에 두 수치가 캐시와 화면에 표시됨. 인증 실패나 상시 결측은 아님.
   - Debug 빌드에서 인증키/URL을 노출하지 않고 `AirQualityRepository` 실패 단계와 예외 유형만 기록하도록 함. 재발 시 이 로그로 측정소 목록/관측값 요청 중 어느 단계가 실패했는지 확인할 것.
5. **초단기 탭 네이버 지도 웹뷰**:
   - `https://weather.naver.com/map?visualMapType=maple` 사용.
   - 외부 웹 의존성이므로 네이버 측 모바일 웹페이지 레이아웃 변경 시 영향 가능성 있음.

---

## Testing and verification

- **단위 테스트**:
  ```bash
  ./gradlew testDebugUnitTest
  ```
  (2026-09-26 기준 단위 테스트 메서드는 총 51개. 기존 WeatherCondition 테스트 메서드 1개에서 8종 × 3가지 속성 = 24회 단언을 실행하므로, 전체 테스트 수와 기존 24회 단언을 혼동하지 말 것. 모든 테스트 통과 상태 유지 필수.)
- **빌드 및 패키징**:
  ```bash
  ./gradlew assembleDebug
  ```
- **실기기 설치 및 실행 검증**:
  ```bash
  adb -s <DEVICE_ID> install -r app/build/outputs/apk/debug/app-debug.apk
  adb -s <DEVICE_ID> shell am start -n com.toki.weather/.MainActivity
  ```
- **화면 캡처 및 시각적 검증**:
  ```bash
  adb -s <DEVICE_ID> exec-out screencap -p > screen.png
  ```
 - 탭 UI 변경 시 반드시 3개 탭 각각의 선택/미선택 상태 스크린샷을 확인하여 캡슐 정렬, 아이콘 겹침, 색상 대비를 육안 검증할 것.
  - 2026-09-26 Galaxy S23에서 초단기예보 병합 후 12~14시 구름/흐림과 강수확률 20~30%, 습도 61%, 3시간 선택의 앱 재실행 후 유지, 내일·모레 및 대기질·링크·하단 탭의 한 화면 표시를 확인함. 기상청 단기예보만 사용하던 이전 화면은 같은 시간대 맑음/0%였음.

---

## Pending work

1. **날씨 탭(Tab 0) 고도화**:
   - 현재 날씨와 가로 스크롤 시간별 예보, 내일·모레 고정 일별 예보를 구현함. 시간별 차트가 필요하면 추후 별도 기획.
   - 추후 7일 예보로 확장할 계획이며, 최신 API 명세와 중기예보 도입 방식을 확인한 뒤 API 클라이언트 확장.
2. **v0.9.x 릴리즈 완료 (2026-09-24)**:
   - `v0.9.0` 태그와 [GitHub Release](https://github.com/GNOJES/tokiweather/releases/tag/v0.9.0)를 게시하고 `tokiweather-v0.9.0.apk`를 첨부함.
   - 기존 v0.8.0 배포 APK와 같은 Android 디버그 인증서로 서명한 APK이므로 덮어쓰기 업데이트가 가능함. 별도 릴리즈 서명키는 아직 없음. 이후 서명키를 바꾸면 기존 설치본과 업데이트 호환성이 끊기므로 서명 정책 변경을 먼저 설계할 것.
   - `v0.9.1` 태그와 [GitHub Release](https://github.com/GNOJES/tokiweather/releases/tag/v0.9.1)를 게시하고 동일한 디버그 인증서의 `tokiweather-v0.9.1.apk`를 첨부함. GitHub에서 다시 받은 APK의 SHA-256이 로컬 검증본과 일치함.
   - `v0.9.2`는 versionCode `10`이며, 날씨 예보·야간 아이콘·화면 배치와 대기질/갱신 신뢰성 개선을 포함한다. release APK도 동일한 디버그 인증서로 서명한다. APK 빌드와 단위 테스트(51개) 완료; [GitHub Release](https://github.com/GNOJES/tokiweather/releases/tag/v0.9.2)와 APK SHA-256은 게시 후 확인한다.
3. **에어코리아 연동 운영 확인**:
   - PM10·PM2.5 조회는 에어코리아로 전환됨. `local.properties`의 `AIRKOREA_API_KEY`는 Decoding 키를 저장하며 Retrofit이 URL 인코딩함. 키를 Git/로그에 포함하지 말 것.
   - [대기오염정보](https://www.data.go.kr/data/15073861/openapi.do)와 [측정소정보](https://www.data.go.kr/data/15073877/openapi.do) 두 서비스의 활용승인이 필요. 2026-09-23 두 서비스 모두 resultCode=00 인증 성공. 측정소 목록 672개 수신 및 서울시청 테스트 좌표의 최근접 중구 측정소 대기질 조회 성공. 첫 대기질 요청은 시간 초과였으나 재시도 성공.
   - 측정소 목록은 프로세스 내 24시간 캐시하고 WGS84 거리 기준 50km 이내 가장 가까운 측정소 선택. 현재 관측이 결측/점검/장애이거나 3시간보다 오래되면 -1(자료 없음) 처리. 대기질 실패가 날씨 갱신을 막지 않도록 유지.
   - 이전 Open-Meteo 수치가 에어코리아로 표시되지 않도록 대기질 DataStore 키를 분리함. 2026-09-23 Galaxy S23(SM-S911N)에 Debug APK 업데이트 설치 후 실기기 검증 완료. 날씨 탭 새로고침 시 GPS 지역 갱신, PM10 37·PM2.5 19 수신/캐시 저장, 화면의 에어코리아 출처 및 수치 표시를 로그와 스크린샷으로 확인. API 직접 조회 검증과 실기기 검증을 구분할 것.
4. **2026-09-24 갱신 신뢰성 변경 실기기 검증**:
   - Galaxy S23에 새 Debug APK 설치 후 날씨 탭 새로고침으로 영등포동7가, 갱신된 현재 날씨, 에어코리아 PM10·PM2.5 표시를 확인. 새로고침 로그에서 두 인증키의 원문/인코딩 형태와 `serviceKey=` 노출이 없음을 확인.
   - 기존 큰 위젯이 남아 있었지만 WorkManager 주기 예약이 없던 문제를 실기기에서 발견. 앱 업데이트 수신 및 앱 실행 시 기존 위젯의 예약을 복구하도록 수정 후 재설치: `toki_weather_periodic_update`가 저장된 30분 주기로 생성됨을 WorkManager DB에서 확인. 앱 재실행 뒤에도 예약 ID·세대·등록 시각이 유지됨.
   - 두 종류 위젯 동시 사용 중 한 종류 삭제, 15/60분 주기, 수동 갱신 실패 안내는 아직 실기기에서 확인하지 못함. 기본형 위젯의 비활성화 브로드캐스트를 ADB로 직접 보내려 했으나 Android가 보호된 브로드캐스트라 거부함.

---

## Agent handoff notes

### 과거 에이전트가 반복해서 실수했던 핵심 교훈
1. **기존 디자인 임의 재창작 금지 (Check Git History First)**:
   - 사용자가 "날씨 선택 시 구름을 빼고 태양과 토끼를 노출해줘"라고 했을 때, 에이전트가 기존 태양 디자인을 확인하지 않고 8갈래 각진 사다리꼴 태양과 손잡는 토끼를 새로 그려 넣어 큰 지적을 받았음.
   - **반드시 기존에 정의된 드로어블(`ic_weather_clear.png`나 이전 커밋의 `ic_tab_weather.xml`) 형태를 계승**하고, 손(앞발) 등 어색한 요소를 임의로 덧붙이지 말 것.
2. **초단기 우산 표정과 빗방울 간격**:
   - 우산 아이콘은 선택/미선택 모두 상단 캐노피에 귀여운 표정이 있어야 함.
   - 빗방울이 우산 캐노피 테두리에 딱 붙으면 답답해 보이므로, 캐노피 외곽과 충분한 간격을 두고 아래로 떨어지도록 배치해야 함.
3. **투명도(Alpha) 오남용 주의**:
   - 선택 상태를 강조하겠다고 미선택 아이콘에 `alpha(0.55f)`를 주면 고해상도 AMOLED 디스플레이에서 아이콘이 씻겨 나간 것처럼 보임.
   - 선택 상태는 **배경 캡슐(Pill)과 토끼 캐릭터의 등장 유무**로 구분하고, 아이콘 자체는 항상 100% 선명해야 함.
